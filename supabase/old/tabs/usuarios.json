{
  "module": "Usuários & Veterinários",
  "tables": {
    "usuarios": {
      "columns": {
        "user_id": "uuid (PK, default gen_random_uuid())",
        "created": "timestamptz (default now())",
        "user_name": "text",
        "user_email": "text (unique, NOT NULL)",
        "foto": "text"
      },
      "constraints": {
        "fk_user_auth": "FOREIGN KEY (user_id) REFERENCES auth.users(id)"
      }
    },
    "veterinarios": {
      "columns": {
        "vet_id": "uuid (PK, default gen_random_uuid())",
        "nome": "text",
        "foto": "text",
        "crmv": "bigint",
        "indice": "smallint",
        "usuario_id": "uuid (FK -> usuarios.user_id)",
        "canil_id": "bigint (FK -> canis.id)"
      }
    },
    "user_roles": {
      "columns": {
        "id": "integer (PK)",
        "name": "varchar"
      }
    },
    "report_types": {
      "columns": {
        "id": "integer (PK)",
        "name": "varchar"
      }
    },
    "user_roles_junction": {
      "columns": {
        "user_id": "uuid (FK -> usuarios.user_id)",
        "role_id": "integer (FK -> user_roles.id)"
      }
    }
  }
}
